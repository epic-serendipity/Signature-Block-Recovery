[pytest]
# Ignore the benchmarks directory in normal test discovery
addopts = -ra --strict-markers --ignore=tests/benchmarks
markers =
    benchmark: mark test as a benchmark (ignored in default runs)
